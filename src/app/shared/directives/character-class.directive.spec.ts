import { CharacterClassDirective } from './character-class.directive';

describe('CharacterClassDirective', () => {
  it('should create an instance', () => {
    const directive = new CharacterClassDirective();
    expect(directive).toBeTruthy();
  });
});
