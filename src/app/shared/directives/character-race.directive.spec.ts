import { CharacterRaceDirective } from './character-race.directive';

describe('CharacterRaceDirective', () => {
  it('should create an instance', () => {
    const directive = new CharacterRaceDirective();
    expect(directive).toBeTruthy();
  });
});
